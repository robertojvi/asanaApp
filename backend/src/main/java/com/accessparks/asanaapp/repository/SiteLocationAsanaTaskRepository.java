package com.accessparks.asanaapp.repository;

import com.accessparks.asanaapp.model.SiteLocationAsanaTask;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SiteLocationAsanaTaskRepository extends JpaRepository<SiteLocationAsanaTask, Long> {
}
